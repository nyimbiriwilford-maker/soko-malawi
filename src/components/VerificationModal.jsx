/**
 * Compatibility export — Phase 2 replaces the old popup with VerificationWizard.
 * Profile / ShopPage keep importing VerificationModal with the same props.
 */
export { default } from './VerificationWizard'
