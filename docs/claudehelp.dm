Mobile header (mobile view only): present search as an icon button aligned next to the notification bell in row 1; tapping the search icon expands the search box (row 2) with a close button. Modern, clean, well-aligned design.

Mobile header (mobile view only): remove the circular box styling around the search and notification icons — present both as plain professional icons on the same row (no boxes/borders).
