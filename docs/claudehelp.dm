In the video call path, NotReadableError is now being thrown from getUserMedia — this started after the quality-selector feature was added. Do NOT attempt a fix yet. First, add temporary console logging (prefixed [CallDebug]) at every call site that invokes getUserMedia or getDisplayMedia for the call feature (search useWebRTC.js, GlobalCallListener.jsx, and any new quality-selector component/file). For each call site log: (1) the constraints object being passed, (2) a stack trace or the calling function's name, (3) whether an existing MediaStream from a prior getUserMedia call is still active (i.e. whether its tracks have been .stop()'d) at the moment this new call fires. Do not modify call logic or stream-teardown logic — logging only. Report back the exact log output from a reproduced failing video call, plus a diff of every file touched.