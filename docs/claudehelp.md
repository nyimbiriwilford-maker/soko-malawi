Redesign the mobile header in src/components/SokoNav.jsx to match this layout exactly:

Row 1: SokoMw logo on the left, notification bell icon on the right. Nothing else.
Row 2: Search bar (full width, rounded, gray background like the image), and a filter/sliders icon button on the right of the search bar.
Remove the district pill from the header entirely — hide it or move it elsewhere.
The header should have a clean white background, no border, minimal padding.
Match the clean minimal look in the reference image.
Run npm run build and confirm it passes."