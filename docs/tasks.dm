Redesign the existing SokoMw Offer Cards in Chat to look modern, premium, and professional, using expert marketplace UI/UX principles. Keep all existing offer functionality, data structure, countdown logic, expiration logic, sending, parsing, and chat behavior unchanged. Focus only on improving the visual design and UX. Make the offer amount the main focal point, clearly display the listing context, optional note, pending status, and **the existing live countdown timer** (e.g. “⏱ Expires in 23:41:08”). The countdown must remain accurate, continue running in real time, survive refresh/chat switching, and transition elegantly to an “Offer expired” state when time runs out. Do not create a second timer system—reuse the existing countdown implementation. Design the card to work beautifully on both mobile and desktop, with clean spacing, typography hierarchy, subtle borders/shadows, professional status treatment, responsive sizing, and tasteful micro-interactions. Avoid excessive colors, gradients, oversized icons, or cartoonish styling. Preserve the existing SokoMw design system and ensure the redesign leaves a clean area for future Accept, Decline, and Counter Offer actions. First inspect the current Offer Card and countdown implementation, then redesign it and actually test the rendered result. Test active countdown, near-expiration, expired state, refresh, chat switching, mobile, and desktop. Do not change unrelated chat features. Run `npm run build` after completion and fix any errors introduced by the redesign.
