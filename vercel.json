{
  "buildCommand": "npm ci && chmod +x node_modules/.bin/vite && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}